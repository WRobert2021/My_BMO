sounddevice
numpy<2.5
scipy
onnxruntime
requests>=2,<3
tqdm>=4,<5
scikit-learn>=1,<2
# Linux Python 3.12+ uses the ONNX-only install in setup.sh because
# tflite-runtime does not publish compatible wheels for those interpreters.
openwakeword==0.6.0; platform_system != "Linux" or python_version < "3.12"
ollama
ddgs>=9.14,<10
Pillow
# Qt Quick/QML runtime. The 6.11.1 ARM64 wheel was verified on the primary
# Raspberry Pi 5 target with Python 3.13.5, Debian 13, and glibc 2.41.
PySide6-Essentials==6.11.1
